<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <!-- Custom AppBar Title Text Appearance -->
    <style name="CustomAppBarTitleStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">30dp</item>
        <item name="android:paddingTop">10dp</item><!-- Adjust the size as needed -->
        <item name="android:textColor">@android:color/black</item> <!-- Optional: Customize color -->
    </style>

    <style name="Theme.RecipeHub" parent="Base.Theme.RecipeHub" />
</resources>